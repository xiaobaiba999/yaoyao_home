server:
  port: 3000

spring:
  datasource:
    url: jdbc:mysql://gateway01.ap-northeast-1.prod.aws.tidbcloud.com:4000/yaoyao_memorial?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=true
    username: 3K7wRbrKq3XuNXn.root
    password: VnVOOxo90nYBkq5V
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: assign_uuid

yaoyao:
  jwt:
    secret: yaoyao-memorial-secret-key-2024-production
    expires-in: 604800000
  admin:
    username: yaoyao
    password: yaoyao20230228
  r2:
    endpoint: ${R2_ENDPOINT:}
    access-key-id: ${R2_ACCESS_KEY_ID:}
    access-key-secret: ${R2_ACCESS_KEY_SECRET:}
    bucket-name: ${R2_BUCKET_NAME:}
    public-url: ${R2_PUBLIC_URL:}
    path-prefix: ${R2_PATH_PREFIX:photos}
  oss:
    endpoint: ${OSS_ENDPOINT:}
    access-key-id: ${OSS_ACCESS_KEY_ID:}
    access-key-secret: ${OSS_ACCESS_KEY_SECRET:}
    bucket-name: ${OSS_BUCKET_NAME:}
    url-prefix: ${OSS_URL_PREFIX:}
    path-prefix: ${OSS_PATH_PREFIX:photos}
  gitee:
    access-token: ${GITEE_ACCESS_TOKEN:}
    owner: ${GITEE_OWNER:}
    repo: ${GITEE_REPO:}
    base-url: https://gitee.com/api/v5
    raw-url: https://gitee.com
    path: images
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:*}
